<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/op1"
        android:checkableBehavior="single">
        <item android:id="@+id/map" android:icon="@drawable/map" android:title="Map View" />
    </group>

    <group
        android:id="@+id/op2"
        android:checkableBehavior="single"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp">
        <item android:id="@+id/profile" android:icon="@drawable/profile" android:title="Profile" />
    </group>

    <group
        android:id="@+id/op3"
        android:checkableBehavior="single">
        <item android:id="@+id/logout" android:icon="@drawable/logout" android:title="Logout" />
    </group>

</menu>